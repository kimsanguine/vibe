# PPT Agent

AI 기반 PPT 자동 생성 에이전트 - Claude + Gemini 멀티 LLM 구조

## 핵심 원칙

- **멀티 LLM 전략**: 논리 태스크(Claude) + 시각 태스크(Gemini) 분리
- **Scaffolding 최소화**: 목표 기반 자율성 부여
- **Glass Box 투명성**: 모든 Agent는 결정 근거 출력
- **Human-in-the-Loop**: 최대 3-4회 사용자 인터랙션

## 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    PPT Agent System                         │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐                                            │
│  │ Orchestrator│ ←── 전체 조율, HITL 관리                   │
│  │   (Claude)  │                                            │
│  └──────┬──────┘                                            │
│         │                                                   │
│  ┌──────┴──────────────────────────────────┐               │
│  │              LLM Router                  │               │
│  │  논리 태스크 → Claude                    │               │
│  │  시각 태스크 → Gemini                    │               │
│  └──────┬──────────────────────────────────┘               │
│         │                                                   │
│  ┌──────┴──────┬──────────────┬─────────────┐              │
│  │             │              │             │              │
│  ▼             ▼              ▼             ▼              │
│ Research    Structure      Design        Export            │
│ (Claude)    (Claude)      (Gemini)      (python-pptx)      │
└─────────────────────────────────────────────────────────────┘
```

## 설치

```bash
# 의존성 설치
pip install -e .

# 또는
pip install -r requirements.txt
```

## 환경 설정

```bash
# .env 파일 생성
cp .env.example .env

# API 키 설정
ANTHROPIC_API_KEY=your_anthropic_api_key
GOOGLE_API_KEY=your_google_api_key  # Phase 2
```

## 사용법

### CLI

```bash
# 기본 사용
ppt-agent "AI 기술 트렌드 2024"

# 출력 파일 지정
ppt-agent "스타트업 투자 유치 전략" -o pitch_deck.pptx

# 청중 및 톤 지정
ppt-agent "기후변화 대응 방안" --audience "정책 결정자" --tone academic

# HITL 비활성화 (자동 진행)
ppt-agent "마케팅 전략" --no-hitl
```

### Python API

```python
from ppt_agent import PPTAgent

agent = PPTAgent()
output_file = agent.run("AI 기술 트렌드 2024")
print(f"생성된 파일: {output_file}")
```

## Phase 구현 현황

| Phase | 상태 | 구성요소 |
|-------|------|----------|
| **Phase 1** | ✅ 완료 | Orchestrator(C) + Research(C) + Export(C) |
| **Phase 2** | ✅ 완료 | Structure(C) + Design(G) 연동 + 시각 평가 |
| Phase 3 | 🔲 예정 | Asset(G) + 이미지 생성 |
| Phase 4 | 🔲 예정 | 전체 HITL + 한국형 템플릿 |

## 워크플로우 (Phase 2)

```
[입력] → [Orchestrator/Claude] → [HITL#1: 요청 확인]
  → [Research/Claude] → [리서치 평가]
  → [Design/Gemini] → [HITL#2: 디자인 선택]
  → [Structure/Claude] → [HITL#3: 구조 확인]
  → [Visual Eval/Gemini] → [Export] → [PPTX 출력]
```

## 프로젝트 구조

```
ppt_agent/
├── __init__.py
├── config.py           # 설정 및 환경변수
├── main.py             # 메인 실행 파일 (8-step workflow)
├── llm/
│   ├── __init__.py
│   ├── router.py       # LLM Router (Claude/Gemini)
│   └── clients.py      # LLM 클라이언트
├── agents/
│   ├── __init__.py
│   ├── base.py         # Base Agent (Glass Box 패턴)
│   ├── orchestrator.py # Orchestrator Agent (HITL 관리)
│   ├── research.py     # Research Agent [Claude]
│   └── design.py       # Design Agent [Gemini]
├── skills/
│   ├── __init__.py
│   ├── structure.py    # 슬라이드 구조 (한국형 기승전결)
│   ├── design.py       # 디자인 시스템 (컬러/폰트/레이아웃)
│   └── export.py       # PPTX 내보내기
└── models/
    ├── __init__.py
    └── slide.py        # 데이터 모델
```

## LLM 라우팅

| 태스크 유형 | LLM | 이유 |
|------------|-----|------|
| orchestrate, research, structure, export | **Claude** | 논리적 추론, 긴 문서 처리 |
| design, asset, visual_eval | **Gemini** | 이미지 생성, 시각적 이해 |

## 라이선스

MIT License
