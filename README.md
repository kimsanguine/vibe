# Vibe

## Installation

### OpenClaw

To install OpenClaw, run the following command:

```bash
curl -fsSL https://openclaw.ai/install.sh | bash
```

### jq

To install jq (a lightweight command-line JSON processor), run the following command:

```bash
brew install jq
```
